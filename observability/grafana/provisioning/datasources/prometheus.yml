apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # Keep the local provisioned UID aligned with Grafana Cloud so the same
    # dashboard datasource variable works in both environments.
    uid: grafanacloud-prom
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
